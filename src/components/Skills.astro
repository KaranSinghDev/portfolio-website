---
// The data structure remains the same, making it easy to manage your skills.
const skillCategories = [
  {
    title: "Programming Languages",
    skills: ["C++", "Python", "SQL"],
  },
  {
    title: "Backend & Databases",
    skills: ["Docker", "Kubernetes", "Kafka", "Redis", "PostgreSQL", "MySQL", "FastAPI", "Elasticsearch"],
  },
  {
    title: "Cloud & DevOps",
    skills: ["Linux", "AWS (EC2, S3, Lambda)", "Azure", "CI/CD", "GitHub Actions"],
  },
  {
    title: "AI & Machine Learning",
    skills: ["PyTorch", "Scikit-learn", "Pandas", "NumPy", "Transformers", "LangChain", "CUDA"],
  },
];
---

<section class="py-20 bg-gray-900">
  <div class="container mx-auto px-6 max-w-5xl">
    <h2 class="text-3xl md:text-4xl font-bold text-center text-white mb-16">
      Technical Skills
    </h2>
    <div class="space-y-10">
      {skillCategories.map(category => (
        <div>
          <h3 class="text-xl font-semibold text-indigo-300 mb-4">{category.title}</h3>
          <div class="flex flex-wrap gap-3">
            {category.skills.map(skill => (
              <span class="bg-gray-800 text-gray-300 text-base font-medium px-4 py-2 rounded-lg">
                {skill}
              </span>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<!--
Key Changes:
- Layout: We've removed the `grid` of cards and are now using a single-column layout with `space-y-10` to separate the categories.
- Tags: Each skill is now a `<span>` element styled to look like a modern "tag" or "pill".
- `flex flex-wrap`: This is the key to the tag cloud. It allows the skill tags in each category to line up horizontally and wrap to the next line if there isn't enough space.
- `gap-3`: This adds a small, consistent space between each tag.
- Styling: The tags have a subtle `bg-gray-800` background and a slightly larger font and padding (`text-base`, `px-4 py-2`) for better readability compared to the project tags.
-->