---
// Array of skill categories. This makes it easy to add/remove skills later.
const skillCategories = [
  {
    title: "Programming Languages",
    skills: ["C++", "Python", "SQL"],
  },
  {
    title: "Backend & Databases",
    skills: ["Docker", "Kubernetes", "Kafka", "Redis", "PostgreSQL", "MySQL", "FastAPI", "Elasticsearch"],
  },
  {
    title: "Cloud & DevOps",
    skills: ["Linux", "AWS (EC2, S3, Lambda)", "Azure", "CI/CD", "GitHub Actions"],
  },
  {
    title: "AI & Machine Learning",
    skills: ["PyTorch", "Scikit-learn", "Pandas", "NumPy", "Transformers", "LangChain", "CUDA"],
  },
];
---

<section class="py-20 bg-gray-900">
  <div class="container mx-auto px-6">
    <h2 class="text-3xl md:text-4xl font-bold text-center text-white mb-12">
      Technical Skills
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {skillCategories.map(category => (
        <div class="bg-gray-800 p-6 rounded-lg shadow-md">
          <h3 class="text-xl font-semibold text-indigo-300 mb-4">{category.title}</h3>
          <ul class="space-y-2">
            {category.skills.map(skill => (
              <li class="text-gray-300">{skill}</li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>

<!--
New Tailwind Classes & Concepts:
- `bg-gray-900`: A slightly lighter shade of dark for the section background to create contrast.
- `mb-12`: margin-bottom for spacing.
- `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4`: This is the core of the layout. It creates a CSS Grid. On mobile, it's a single column (`grid-cols-1`). On medium screens, it becomes two columns (`md:grid-cols-2`), and on large screens, it's four columns (`lg:grid-cols-4`).
- `gap-8`: Sets the spacing between grid items.
- `rounded-lg shadow-md`: Styles for the individual skill cards.
- { ... }: This is how you write JavaScript expressions and loops inside Astro's HTML-like syntax. We are looping through the `skillCategories` array to generate the cards dynamically.
-->